An until loop will iterate while a test condition fails but its do done block must contain a statement that will at some point make the test succeed so the loop will end.</br>


```
initializer
until conditional test becomes true
do
    statements to execute
    incrementer
done
```
