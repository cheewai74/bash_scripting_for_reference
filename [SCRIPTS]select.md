A select in statements generates a menu of options then executes statements within a do done block upon selection</br>

The select statement performs a loop by default but this can be supressed using the break keyword</br>

```
select item in list
do
    statements to execute
done
```
